{
  "description": "Test fixture for filtering questions by provider only",
  "method": "GET",
  "endpoint": "/v1/questions", 
  "queryParams": {
    "provider": "aws",
    "limit": "25"
  },
  "expectedStatus": 200,
  "expectedFields": [
    "questions",
    "total",
    "filters",
    "pagination"
  ],
  "validation": {
    "maxQuestions": 25,
    "requiredValues": {
      "providerId": "aws"
    },
    "variableFields": ["examId", "topicId", "difficulty", "type"]
  }
}