{
  "description": "Test fixture for listing exams filtered by certification level (associate)",
  "method": "GET",
  "endpoint": "/v1/exams",
  "queryParams": {
    "level": "associate"
  },
  "expectedStatus": 200,
  "expectedFields": [
    "exams",
    "total",
    "pagination",
    "filters",
    "aggregations"
  ],
  "validation": {
    "minExams": 2,
    "allExamsMatchLevel": "associate",
    "requiredExamFields": [
      "id",
      "name",
      "level"
    ]
  }
}