{
  "description": "Test fixture for listing exams filtered by category (cloud)",
  "method": "GET",
  "endpoint": "/v1/exams",
  "queryParams": {
    "category": "cloud"
  },
  "expectedStatus": 200,
  "expectedFields": [
    "exams",
    "total",
    "pagination",
    "filters",
    "aggregations"
  ],
  "validation": {
    "minExams": 4,
    "allExamsMatchCategory": "cloud",
    "requiredExamFields": [
      "id",
      "providerId",
      "name",
      "metadata"
    ]
  }
}