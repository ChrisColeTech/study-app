import React from 'react'

interface ExamProviderSelectorProps {
  className?: string
}

export const ExamProviderSelector: React.FC<ExamProviderSelectorProps> = ({ className = '' }) => {
  return (
    <div className={`ExamProviderSelector ${className}`}>
      {/* TODO: Implement ExamProviderSelector */}
    </div>
  )
}
